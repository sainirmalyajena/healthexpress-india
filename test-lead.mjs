import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const lead = await prisma.lead.findFirst({ where: { referenceId: 'HE-MSOLFXC9-A0GJ' }}); console.log(lead); } main().catch(console.error).finally(() => prisma.$disconnect());